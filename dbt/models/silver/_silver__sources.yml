version: 2

sources:
  - name: raw
    description: Bronze landing zone produced by the synthetic data generator.
    database: skullport
    schema: raw
    tables:
      - name: labels
      - name: tracking_events
