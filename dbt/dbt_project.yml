name: skullport
version: "1.0.0"
config-version: 2

profile: skullport

model-paths: ["models"]

models:
  skullport:
    # Materialize as Delta tables (full refresh; incremental is a future improvement)
    +materialized: table
    silver:
      +schema: silver
    gold:
      +schema: gold
